import '@emotion/react';

declare module '@emotion/react' {
    export interface Theme {
        color: {
            buttonYellow: string;
            yellow: string;
            white: string;
            black: string;
            dark: string;
            darkGray: string;
            orange: string;
        };
    }
}
